# Local demo product images (source for DummyCatalogSeeder).
# Kept out of version control — they are temporary placeholder photos.
# To seed on another machine, copy this folder there, or replace with real assets.
/dummy-products/

# OS / editor cruft
.DS_Store
Thumbs.db

# Stray local spreadsheets
farnid.xlsx
*.xlsx

# SMS gateway scratch notes — these hold a live Automas API key. Never commit.
/docs/sms-gateway/notes.txt
/docs/sms-gateway/code-examples.txt

# Playwright MCP scratch output
/.playwright-mcp/
